name: first
on:
  push:
  workflow_dispatch:
jobs:
  job1:
    name: first job
    runs-on: ubuntu-latest
    environment: test-env
    steps:
      - name: step one
        uses: actions/checkout@v4
      - name: step two
        run: env | sort
  job2:
    name: second job
    runs-on: windows-latest
    environment: test-env
    steps:
      - name: step one
        uses: actions/checkout@v4



